# gitbook-plugin-betterpagetitles

GitBook Plugin to add the page title just before the content on every page.

```js
{
  "plugins": ["betterpagetitles"]
  }
}

```

Then run `gitbook install` to download and install the plugin.
